# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="slownessscout",  # Must be unique
    name="SlownessScout",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="slownessscout_app",
    stack_size=4 * 1024,
    sources=["slownessscout.c", "slownessscout_lib.c"],
    fap_category="Tools",
    # Optional values
    fap_version=(0, 1),
    fap_description="Frame time & input latency monitor",
    fap_author="User",
    fap_icon="slownessscout.png",  # 10x10 1-bit PNG
    fap_icon_assets="images",  # Image assets to compile for this application
)
